platform :ios, '9.0'

target 'RSSFeed' do
  use_frameworks!

  # Pods for RSSFeed
  pod 'Alamofire', '~> 4.6'
  pod 'SwiftyJSON', '~> 4.0.0'
  pod 'PINRemoteImage', '~> 2.1.4' 
  pod 'SnapKit', '~> 4.0.0'

  target 'RSSFeedTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
